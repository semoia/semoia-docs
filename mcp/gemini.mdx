---
title: "Gemini CLI"
description: "Manual configuration for Gemini CLI"
---

Add this to your `mcp_config.json`:

```json
{
  "mcpServers": {
    "opennote": {
      "command": "opennote",
      "args": ["mcp", "run"]
    }
  }
}
```
