---
title: "Other Clients"
description: "Manual Configuration for other AI agents"
---

### Trae

**Remote Server (Recommended):**

```json
{
  "mcpServers": {
    "semoia": {
      "url": "https://semobeta.vercel.app/mcp",
      "headers": {
        "Authorization": "Bearer YOUR_API_KEY"
      }
    }
  }
}
```

---

### BoltAI

- Go to **Settings → MCP**
- Add a new server with:
  - **Type**: HTTP
  - **URL**: `https://semobeta.vercel.app/mcp`
  - **Headers**: `Authorization: Bearer YOUR_API_KEY`

---

### Gemini CLI

Add to your `mcp_config.json`:

**Remote Server (Recommended):**

```json
{
  "mcpServers": {
    "semoia": {
      "serverUrl": "https://semobeta.vercel.app/mcp",
      "headers": {
        "Authorization": "Bearer YOUR_API_KEY"
      }
    }
  }
}
```

---

### Roo Code

Add to your Roo Code MCP configuration:

**Remote Server (Recommended):**

```json
{
  "mcpServers": {
    "semoia": {
      "url": "https://semobeta.vercel.app/mcp",
      "headers": {
        "Authorization": "Bearer YOUR_API_KEY"
      }
    }
  }
}
```
