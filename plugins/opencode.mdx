---
title: "OpenCode Plugin"
description: "Connect OpenNote with OpenCode"
---

## Install
```bash
opennote plugins install opencode-plugin
```

## What it does
- Loads OpenNote context inside OpenCode
- Works locally with the OpenNote API

## Notes
- Requires OpenNote web running (`opennote start`)
